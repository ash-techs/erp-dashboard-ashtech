{
  "name": "webauthn-backend-prisma",
  "version": "1.0.0",
  "description": "WebAuthn backend server with Node.js, Express, and Prisma ORM for PostgreSQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["webauthn", "authentication", "biometric", "fingerprint", "prisma", "postgresql"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@simplewebauthn/server": "^13.1.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "uuid": "^11.0.4"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
